[package]
name = "tester"
version = "0.1.0"
license.workspace = true
edition.workspace = true

[dependencies]
etherparse = "0.19.0"
push-packet = { version = "0.1.0", path = "../push-packet", features = ["build-ebpf"] }
